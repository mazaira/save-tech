<ul class="list-group">
  <% tags&.each do |tag| %>
  <%= link_to tag.name, filter_items_path(name: tag.name), :remote => true %>
  <% end %>
</ul>

<!-- <script>
  $('.tag').each(function(){
          $(this).css({"left": Math.random() * window.outerWidth , "top": Math.random() * window.outerHeight}).textillate();
  });
</script> -->
